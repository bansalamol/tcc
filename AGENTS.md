# Repository Guidelines

- Run `npm install` and `npm run build` after changes to front-end assets.
- Attempt to run `vendor/bin/phpunit --version` to show PHP tools; note it may fail if PHP is missing.
- Keep code styling consistent with Tailwind and Blade templates already present.
- Summarize commits clearly.
